import { EmptyState } from '../components/EmptyState'
export function ResourcePage({title,description}:{title:string;description:string}){return <div className="space-y-6"><header><p className="eyebrow">NetScope workspace</p><h1 className="mt-1 text-3xl font-bold tracking-tight">{title}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slateblue">{description}</p></header><div className="card p-5"><EmptyState title={`No ${title.toLowerCase()} available`} detail="This view is ready for organization-scoped API data. Missing data is shown as inconclusive, never as healthy."/></div></div>}
