import { ChevronRight, CircleHelp, FileKey2, ShieldAlert } from 'lucide-react'
import { useParams } from 'react-router-dom'
import { EmptyState } from '../components/EmptyState'
import { StatusBadge } from '../components/StatusBadge'
const questions=[['What was observed?','No source observation is available.'],['What does it mean?','A conclusion cannot be made without normalized evidence.'],['Why does it matter?','Contextual impact remains unassessed.'],['Suggested action','Collect authorized evidence and review confidence before prioritizing.']]
export function FindingDetailPage(){const {id}=useParams();return <div className="space-y-6"><header><p className="eyebrow">Finding detail</p><div className="mt-2 flex flex-col justify-between gap-3 md:flex-row md:items-center"><div><h1 className="text-3xl font-bold">Finding {id ?? 'not selected'}</h1><p className="mt-2 text-sm text-slateblue">No finding record was returned for this workspace.</p></div><StatusBadge status="INCONCLUSIVE"/></div></header><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{[['Status','Open'],['Priority','Unassessed'],['Severity','Unassessed'],['Confidence','Low'],['Affected asset','Unknown']].map(([label,value])=><div className="card p-4" key={label}><p className="text-xs font-semibold uppercase text-slateblue">{label}</p><p className="mt-2 font-bold">{value}</p></div>)}</section><section className="grid gap-5 xl:grid-cols-[1.35fr_1fr]"><div className="space-y-4">{questions.map(([title,body])=><article className="card p-5" key={title}><h2 className="font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-slateblue">{body}</p></article>)}</div><aside className="space-y-5"><div className="card p-5"><div className="flex items-center gap-2"><CircleHelp className="text-brand" size={19}/><h2 className="font-bold">Why am I seeing this?</h2></div><p className="mt-3 text-sm leading-6 text-slateblue">This panel lists the modules and evidence that produced a conclusion. No correlation chain is present.</p></div><div className="card p-5"><div className="flex items-center justify-between"><div className="flex items-center gap-2"><FileKey2 className="text-brand" size={19}/><h2 className="font-bold">Technical Evidence</h2></div><ChevronRight size={18}/></div><div className="mt-4"><EmptyState title="No evidence attached" detail="Raw artifacts are permission-controlled and never made public."/></div></div><div className="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-900"><div className="flex gap-2"><ShieldAlert className="mt-0.5 shrink-0" size={18}/><p>Severity is not the same as contextual risk. Vulnerability detection is not proof of compromise.</p></div></div></aside></section></div>}
