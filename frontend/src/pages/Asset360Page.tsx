import { Activity, ArrowRight, Clock3, Globe2, Network, Server, ShieldCheck, Signal, Waypoints } from 'lucide-react'
import { useParams } from 'react-router-dom'
import { EmptyState } from '../components/EmptyState'
import { StatusBadge } from '../components/StatusBadge'
const tabs=['Overview','Connectivity','Route','Services','Traffic','Security','Performance','Timeline','Evidence']
export function Asset360Page(){const {id}=useParams();return <div className="space-y-6"><section className="card overflow-hidden"><div className="bg-gradient-to-r from-navy to-[#1e3d68] p-6 text-white"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-start"><div className="flex gap-4"><div className="grid h-12 w-12 place-items-center rounded-xl bg-white/10"><Server/></div><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-blue-200">Asset 360</p><h1 className="mt-1 text-2xl font-bold">Asset {id ?? 'not selected'}</h1><p className="mt-2 text-sm text-slate-300">No technical identity has been observed yet.</p></div></div><StatusBadge status="INCONCLUSIVE"/></div></div><nav className="flex overflow-x-auto border-b border-line px-4" aria-label="Asset views">{tabs.map((tab,index)=><button key={tab} className={`focus-ring shrink-0 border-b-2 px-4 py-4 text-sm font-semibold ${index===0?'border-brand text-brand':'border-transparent text-slateblue'}`}>{tab}</button>)}</nav></section><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[['Availability','Unknown',Signal],['Latency','No samples',Activity],['Services','Not inventoried',Network],['Security','No conclusions',ShieldCheck]].map(([label,value,Icon])=><article className="card p-5" key={label as string}><Icon className="text-brand" size={20}/><p className="mt-5 text-xs font-semibold uppercase tracking-wide text-slateblue">{label as string}</p><p className="mt-1 text-lg font-bold">{value as string}</p></article>)}</section><section className="grid gap-5 xl:grid-cols-[1.4fr_1fr]"><div className="card p-5"><div className="flex items-center justify-between"><div><p className="eyebrow">Connectivity path</p><h2 className="mt-1 text-lg font-bold">Route and service reachability</h2></div><Waypoints className="text-slateblue"/></div><div className="mt-5 flex items-center gap-3 rounded-xl border border-dashed border-line bg-panel p-5 text-sm text-slateblue"><div className="rounded-lg bg-white p-2 shadow-sm"><Globe2 size={18}/></div><ArrowRight size={16}/><div className="rounded-lg bg-white p-2 shadow-sm"><Server size={18}/></div><span>Awaiting an authorized route observation</span></div><div className="mt-5"><EmptyState title="Technical evidence has not been collected" detail="Asset 360 never treats missing evidence as proof of health. Inconclusive remains visible until trustworthy observations exist."/></div></div><aside className="card p-5"><div className="flex items-center gap-2"><Clock3 size={18} className="text-brand"/><h2 className="font-bold">Recent timeline</h2></div><div className="mt-5 border-l border-line pl-5"><p className="text-sm font-semibold">Asset workspace created</p><p className="mt-1 text-xs leading-5 text-slateblue">Observations, service changes, findings and evidence will appear here.</p></div></aside></section></div>}
