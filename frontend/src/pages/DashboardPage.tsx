import { AlertTriangle, Boxes, CircleHelp, Clock3, Globe2, RadioTower, ShieldAlert, Workflow } from 'lucide-react'
import { EmptyState } from '../components/EmptyState'
import { StatusBadge } from '../components/StatusBadge'
const metrics=[['Assets monitored','0','Inventory is ready',Boxes],['Agents online','0','No agents enrolled',RadioTower],['Open findings','0','No findings reported',ShieldAlert],['Jobs running','0','Queue is idle',Workflow],['Public assets','0','Only approved scopes appear',Globe2],['Failed diagnostics','0','No diagnostic history',AlertTriangle],['TLS expiring','0','No certificate evidence',Clock3],['Inconclusive checks','0','Valid outcome, not a pass',CircleHelp]] as const
export function DashboardPage(){return <div className="space-y-7"><section className="flex flex-col justify-between gap-3 md:flex-row md:items-end"><div><p className="eyebrow">Operations overview</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-ink">Network clarity, grounded in evidence.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slateblue">A calm view of connectivity, exposure and operational state. Values remain empty until an authorized agent submits evidence.</p></div><StatusBadge status="INCONCLUSIVE"/></section><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([label,value,detail,Icon])=><article key={label} className="card p-4"><div className="flex items-center justify-between"><div className="rounded-lg bg-blue-50 p-2 text-brand"><Icon size={18}/></div><span className="text-2xl font-bold tabular-nums">{value}</span></div><h2 className="mt-4 text-sm font-semibold">{label}</h2><p className="mt-1 text-xs text-slateblue">{detail}</p></article>)}</section><section className="grid gap-5 xl:grid-cols-[1.45fr_1fr]"><div className="card p-5"><div className="mb-5 flex items-center justify-between"><div><p className="eyebrow">Network health</p><h2 className="mt-1 text-lg font-bold">Availability and response</h2></div><span className="text-xs text-slateblue">Last 24 hours</span></div><EmptyState title="No monitoring evidence yet" detail="Enroll a NetScope Agent, approve a scope and schedule a safe diagnostic module to establish a baseline."/></div><div className="card p-5"><p className="eyebrow">Priority review</p><h2 className="mt-1 text-lg font-bold">What needs attention</h2><div className="mt-5"><EmptyState title="Nothing to review" detail="Findings will show severity, contextual priority, confidence and the evidence behind each conclusion."/></div></div></section></div>}
